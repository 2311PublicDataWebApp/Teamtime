<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teamtime.tt.user.model.mapper.UserMapper">
	<select id="selectUserById" resultType="User">
		SELECT * FROM USER_TBL WHERE USER_ID = #{userId }
	</select>
	<insert id="insertUser">
        INSERT INTO USER_TBL VALUES(#{userId }, #{userPw }, #{userName }, #{userNickname }, #{userEmail }, #{userPhone }, #{userGender }, 'NULL', 'USER')
    </insert>
    <update id="updateUser">
    	UPDATE USER_TBL SET USER_EMAIL = #{userEmail }, USER_NICKNAME = #{userNickname } WHERE USER_ID = #{userId }
    </update>
</mapper>